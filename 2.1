// Eloquent JavaScript Exercise 2.1

// start with string = #
// continue to add # until length = given amount

var triangleSize = prompt("How big should this triangle be?");

for(var triangle = "#"; triangle.length <= triangleSize; triangle += "#") {
  console.log(triangle);
}
